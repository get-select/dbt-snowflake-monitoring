version: 2

models:
  - name: monthly_spend
    tests:
      - advanced_equality:
          # fixing for fusion, not sure about backwards compatibility
          arguments:
            compare_model: ref('monthly_spend_fixture'),
            round_columns: ['spend']
